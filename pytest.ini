[pytest]
addopts = -p no:cacheprovider --strict-markers
testpaths = tests
markers =
    unit: fast isolated unit test
    integration: integration test requiring explicit selection
    performance: performance test requiring explicit selection
    gpu: test requiring a CUDA-capable GPU
